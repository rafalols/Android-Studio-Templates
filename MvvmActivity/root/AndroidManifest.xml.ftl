<manifest xmlns:android="http://schemas.android.com/apk/res/android"
	package="${applicationPackage}">

    <application>
        <activity android:name="${packageName}.${lowerCaseName}.${activity}" />
    </application>

</manifest>
