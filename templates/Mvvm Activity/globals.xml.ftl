<?xml version="1.0"?>
<globals>

    <global id="lowerCaseName" value="${screenName?lower_case}" />

    <global id="layoutName" value="activity_${screenName?lower_case}" />
    <global id="itemLayoutName" value="viewholder_${screenName?lower_case}" />

    <global id="activity" value="${screenName}Activity" />
    <global id="viewModel" value="${screenName}ViewModel" />
    <global id="viewBinding" value="Activity${screenName}Binding" />
    <global id="adapter" value="${screenName}Adapter" />
    <global id="viewHolder" value="${screenName}ViewHolder" />
    <global id="item" value="${screenName}Item" />
    <global id="itemBinding" value="Viewholder${screenName}Binding" />

</globals>
